{
  "name": "ethers-eval-error",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230228.0",
    "typescript": "^4.9.5",
    "wrangler": "2.12.0"
  },
  "private": true,
  "scripts": {
    "dev": "wrangler dev --local",
    "build": "wrangler publish --dry-run --outdir dist"
  }
}
